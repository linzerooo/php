<table>
    <tr>
        <th>Пн</th><th>Вт</th><th>Ср</th><th>Чт</th><th>Пт</th><th>Сб</th><th>Вс</th>
    </tr>
    <tr>
        {% for day in days %}
            {% if day.date|date('N') == 1 and not loop.first %}</tr><tr>{% endif %}
            <td{% if day.isWeekend %} style="color: red;"{% endif %}>{{ day.date|date('d') }}</td>
        {% endfor %}
    </tr>
</table>
